language: cpp
dist: trusty
sudo: false
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      - george-edison55-precise-backports
    packages:
      - clang
      - cmake-data
      - cmake
compiler:
  - clang
install:
  - >-
    mkdir build;
    cd build;
    cmake .. -DBUILD_CASCTESTS=on;
    make -j2;
script:
  - make $TEST_TYPE;
